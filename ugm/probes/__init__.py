"""Worlds and measured questions: FINDINGS, not release criteria.

Each names a question in its own docstring and answers it by running something.
A probe going red has discovered something and wants reading; it does not
necessarily block a release, which is what separates these from `gates`.

⚠ A probe whose question is SETTLED is a candidate for retirement rather than a
permanent fixture -- `ugm.modality` says so in its own header.
"""
