"""What the agent learns from use, and the measurements that say whether it did.

Nothing in `core` imports anything here. That is the point of the split: an
agent runs without any of it, and every module below is a claim about whether
carrying experience forward pays.
"""
