__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.mypy_cache/
*.egg

# Third-party research corpora fetched by bench/fetch_corpora.py — NOT ours to redistribute.
data/corpora/

# Claude Code's agent worktrees live here. They are checkouts of THIS repo, so
# `git add -A` from the top otherwise stages one as an embedded gitlink.
.claude/
